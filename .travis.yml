build: Script
language: cpp
compiler:
    - clang++
    - g++
os:
    - osx
    - linux
addons:
    apt:
        packages:
            - cmake
matrix:
    fast_finish: true
before_script:
    - cmake --version
    - $CXX --version
script:
    - cmake .
    - make -j 2
    - make test
